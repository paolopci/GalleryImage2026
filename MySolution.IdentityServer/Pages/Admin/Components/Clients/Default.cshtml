@model MySolution.IdentityServer.Pages.Admin.Components.Clients.ClientsViewModel

<div class="card h-100 text-bg-light">
    <h5 class="card-header">
        <i class="bi bi-laptop me-2"></i>
        Clients
        <a asp-page="/Admin/Clients/Index" class="float-end">
            <i class="bi bi-plus-circle"></i>
            <span class="d-none">Add Clients</span>
        </a>
    </h5>
    <div class="card-body">
        <p class="display-2 mb-0 text-center">
            @Model.Count
        </p>
    </div>
</div>
